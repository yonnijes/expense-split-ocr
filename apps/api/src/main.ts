// Skeleton NestJS bootstrap (MVP)
async function bootstrap() {
  // TODO: inicializar NestFactory + módulos
  // eslint-disable-next-line no-console
  console.log('ExpenseSplit OCR API placeholder running');
}

bootstrap();
